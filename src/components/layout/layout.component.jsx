import React from 'react';

const Layout = ( content ) => (
    <div className='container'>
         <div className='title'>
             ${content.title}
        </div>
    </div>
)
export default Layout;